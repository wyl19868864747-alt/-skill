---
name: linfeng-image-prompt
description: 分析、梳理、撰写、优化并执行图片生成与图片编辑任务，也可按需寻找、筛选并重构图片灵感、热门提示词与示例案例，并从用户修正与生成复盘中提炼、验证和维护可复用经验。用于用户要求生图、生成图片、创建图片、重绘、改图、扩图、融合参考图、找生图灵感/风格案例、写或优化生图提示词、诊断图片不够电影感/太暗/太白/太脏/不清晰/人物或构图不一致，以及制作真人电影感、真人身份参数卡、身材比例锁定、角色设定图、带面部特写的角色四视图、场景图、场景多角度图、反向机位图、俯视方位图、动漫插画、电商产品图、数字人商业图、数字人手持或使用商品图时；尤其适合需要锁定人物五官、服装、体型比例、商品事实与尺度、商品可见角度、人物和商品接触关系、空间结构、机位构图、光影色彩、多张参考图职责或持续优化生图工作流的任务。
---

# 林枫生图

把“理解任务—锁定约束—整理画面—生成或改写—复盘成长”作为一条连续工作流。先解决画面关系，再添加风格词；先保证不可变内容，再提升质感。

## 每次开始

1. 完整阅读 `references/prompt-framework.md`，按其中的画面结构和质量规则工作。
2. 完整阅读 `references/active-learnings.md`，应用已经生效的长期经验。
3. 当任务涉及局部修改、参考重绘、扩图、人物连续性、场景补全、背景复杂度、群像、物理关系、商品尺度、品牌资产、真人表演或视频资产用途时，完整阅读 `references/visual-decision-principles.md`，只应用与当前问题直接相关的决策卡。
4. 只有用户明确要求找灵感、找案例、找热门提示词或给多个视觉方向时，才完整阅读 `references/inspiration-retrieval.md`；普通直接生图、改图和明确提示词任务不得加载。
5. 任务包含真实商品、电商产品、商品广告图或商品参考图时，完整阅读 `references/product-preflight.md`，先判断商品事实、证据边界、尺度、可见角度和资产缺口。
6. 任务属于电商、商品广告或种草图时，完整阅读 `references/commercial-product-routing.md`，只选择当前图片真正承担的商业模块和视觉价值；非商业生图不得加载该知识库。
7. 任务同时包含真人/数字人与商品，并要求手持、使用、推荐、递出或场景体验时，完整阅读 `references/digital-human-product.md`，锁定人物、商品、接触、遮挡、尺度和受光关系。
8. 首次创建真实实拍人物，或重新定义人物身份、体型和身材比例时，完整阅读 `references/person-identity.md`，先建立人物身份参数卡再生图。已有确认人物的后续镜头直接复用，不重复打断流程。
9. 只有用户明确要求生成“人物/角色四视图”或“带面部特写的四视图”时，才完整阅读 `references/character-sheet.md`，执行其中的版式、身份锁定和真人摄影规则。普通角色设定图、单张定妆照、人物资产图、场景四视图或其他生图任务不得加载该专项标准。
10. 只有在主视图场景已经生成或提供，且用户明确要求该场景的多角度、反向机位、正反打或俯视方位图时，才完整阅读 `references/scene-angles.md`。普通场景图不得加载该专项标准。
11. 用户本轮提出明确修正、解释修改原因、反馈失败、要求“以后注意/加入 Skill/总结优化”时，先完成当前任务，再完整阅读 `references/learning-system.md`、`references/learning-inbox.md` 和 `references/growth-ledger.md`，判断记录、合并、晋级或退役；普通任务不得读取未验证候选。
12. 当前用户明确要求始终高于参考图、历史经验和默认规则。不要让成长规则覆盖本轮指令。

## 判断任务类型

- 用户说“找灵感、找参考案例、找热门提示词、给三种视觉方向”：进入灵感检索；最多返回三个带示例图且路线不同的候选，用户选择后再用本技能重构。用户授权“你直接选并生成”时不增加二次确认。
- 用户说“生成图片、直接出图、创建图片、渲染、重绘、重新生成”：整理后直接调用图片生成能力，不先输出冗长分析，不重复确认已明确的内容。例外：首次创建真实实拍人物且身份参数尚未形成时，先执行人物身份参数卡流程。
- 用户说“改图、换衣、换背景、修复、扩图、融合”：先判定原图编辑、参考重绘或扩图中的唯一模式；查看全部目标图片，明确保留区、变化区与参考职责，再调用图片编辑能力。
- 用户说“写提示词、改成提示词、反推提示词”：只交付可复制的最终提示词，除非用户同时要求出图。
- 用户说“为什么不对、太暗、太白、太脏、人物变了、机位错了”：先诊断主因，再修正提示词或按用户授权重新生成；不要用更多风格词掩盖结构错误。

必要参考图缺失、多个目标人物无法辨认或关键输出比例会改变构图时，才问一个最小化问题。其余缺口按合理视觉逻辑补全。

## 商业生图路由

只有电商、广告、种草、商品展示和商业数字人任务进入本节；纯人物、剧情资产、场景设定和艺术创作继续使用原生图流程。

1. 先判断本图属于单独商品、单独商业人物，还是人物与商品互动。人物出现不等于人物是主角；商品广告中人物通常承担使用证明、尺度参照或情绪投射。
2. 商品信息分为 `已确认`、`可安全推断`、`无法确认`。图片与包装能直接证明的结构、颜色、可见材质和使用状态可写；功效、认证、性能、成分、耐用性和精确尺寸不得从外观编造。
3. 先对照品类的成交理由与头号犹豫，再决定本图承担 Hero、Usage、Material、Lifestyle、Dimension、Trust 等哪个模块。一张图优先承担一个主模块，最多增加一个辅助模块。
4. 只有当缺失信息既属于本品类的核心成交理由或头号犹豫，又会实质改变画面策略时才追问；一次集中询问最高杠杆的 2–3 项，并说明它们会激活什么画面。用户不回答时降级为保守版本，不编造。
5. 商品参考图是外观唯一依据。单张正面图默认只展示正面或轻微三分之四角度；侧面、背面、开盖、内部结构、液体流出、安装拆卸或 360° 展示只有在素材足够时才设计。
6. 尺度判断只决定手持可能性、人物互动、推荐景别和空间占用，不凭图片猜测精确厘米数。大型家具、机器、健身器械和清洁设备不得被设计成桌面小物或手持产品。
7. 认证标志、参数表、复杂 Logo 和精确屏幕文字需要准确时，保留参考资产或干净合成区，不依赖纯生成重写。

## 真人身份参数卡（生图前按需激活）

- 首次创建真实实拍人物，或用户要求换人、重设身份、显著改变身高体型或身材比例时，先执行本节；动漫、CG、背景路人和已有确认人物的后续镜头不重复执行。
- 在实际生图前形成参数卡：身份、核心气质、性别/性别表达、人种/族裔设定、年龄、身高、体型、发型、发色、脸型、眼型与瞳色、肤色、服装样式、服装质感、鞋履、饰品，以及身材比例。
- 参数卡按上述字段逐项独立输出，不省略、不合并；例如年龄不能并入身份，发色不能并入发型。信息不足时明确写“用户未指定”或“设计建议值”。
- 身材比例必须具体到头身比、肩胯关系、上身/下身比例、腿长、手臂长度、手脚尺度、腰线位置、站姿重心与体态，不能只写“比例自然、身材好”。
- 用户信息较少时，先提出一套合理的建议参数卡并等待确认，再调用图片生成；用户已经给出完整参数，或明确授权“自由设计/按你设定直接生成”时，可以先形成参数卡再继续，无需重复提问。
- 参考图只能提供可见外形锚点。没有可靠依据时，不从外貌擅自断言人种、族裔、性别身份或精确身高；这些字段标为“用户未指定/设计值”，需要时再询问。
- 一旦参数确认，后续同一人物的定妆照、四视图、场景图和分镜资产都复用同一参数卡；用户只修改一个字段时，不连带重设计其他字段。
- 完整参数模板、比例判断、参考图边界与摄影防变形规则见 `person-identity.md`。

## 四视图专项（按需激活）

- 仅当用户明确要求生成“人物/角色四视图”或“带面部特写的四视图”时执行本节；否则完整跳过，不把该版式、站姿、白棚背景或转面角度带入普通任务。场景四视图改由场景多角度专项处理。
- 专项激活后，把版式视为结构硬约束：左侧约 40% 为正面脸部大特写，右侧约 60% 为同一人物的正面、严格 90° 侧面、背面全身视图。
- 四个画面是同一人物的同步视角，不是四位相似角色。先建立唯一身份母版，再锁定脸型、五官几何、发际线、肤色、体态、服装版型、材质、配饰和鞋履。
- 用户只要求通用提示词模板且没有提供人物参考或身份描述时，保留清楚的身份锚点填写位，不擅自指定性别、族裔、年龄或具体五官；只有用户要求自由设计或直接生成时才合理补全。
- 真人任务使用 `character-sheet.md` 的光学与生理真实规则；非真人任务保留版式、角度和一致性规则，只替换主风格与对应材质表现。
- 不把“4K、300 PPI”当作清晰度风格词堆叠。提示词写实际像素画布；工具支持导出密度时再设置 PPI，并如实区分生成清晰度与文件元数据。

## 场景多角度专项（按需激活）

- 仅当主视图场景已经生成或由用户提供，并且用户明确要求多角度、反向视角、正反打或俯视方位图时执行本节；否则完整跳过，不改变普通场景图的构图与交付形式。
- 先把已确认主视图定义为唯一场景母版，识别并锁定建筑边界、门窗、固定家具、主要道具、材质、风格、色彩、光源方向和空间尺度，再生成其他角度。
- 默认采用扩展画布：左侧约 75% 保留主视图，右侧约 25% 上方放置反向机位图、下方放置俯视方位图。优先扩展为约 21:9，避免压缩、拉伸或裁切原主视图。
- 反向机位不是水平镜像：若主机位 C1 位于南侧朝北拍摄，则反向机位 C2 位于北侧朝南拍摄；所有门窗、家具和光源必须按现实方位换边显示。
- 俯视图默认北上、东右、南下、西左，标出北/东/南/西及 C1、C2 的位置和朝向。纯生成文字不可靠时，保留干净标注位并使用确定性后期文字覆盖。
- 完整版式、空间母版、不可见区域补全与提示词模板见 `scene-angles.md`。

## 先建立内部约束卡

生成前在内部整理四类信息，不必默认展示给用户：

1. **不可变**：人物身份与五官、发型、服装、产品结构、场景拓扑、机位、构图、画幅、关键道具数量等。
2. **可变化**：动作、表情、天气、光影、色调、景别、背景细节等本轮允许修改的内容。
3. **可推断**：用户未指定但可由视觉逻辑安全补全的内容。
4. **禁止项**：不得新增、删除、替换、变形或误解的元素。

多张参考图必须先分配唯一职责，例如：人物身份、服装、场景、构图/机位、光影/色彩、产品结构。禁止把一张图的构图和另一张图的空间结构无意混合。

参考优先级：本轮文字硬约束 > 本轮目标图及明确参考职责中的可见事实 > 已确认的项目连续性资产 > 当前触发的专项 reference > `visual-decision-principles.md` > `active-learnings.md` > 外部灵感案例 > 默认审美补全。低优先级规则不得覆盖高优先级要求；外部案例只能贡献风格、构图、光色和材质方向，不能作为人物、商品或功效事实。

场景图若用于后续视频生成、人物走位或镜头扩展，先把它视为“场景母图”而非单个成片镜头。默认采用稍远的完整环境景别，让画面覆盖更多可识别的空间区域、固定陈设、出入口、主体活动区和前中后景关系，为后续近景、远景和镜头后拉提供共同的视觉依据。用户口语中的“广角一点”若语义是“展示更多画面信息”，优先落实为信息覆盖与完整取景，不机械增加超广角焦段、房间尺寸、建筑拓扑或无关相机参数。

## 整理生成指令

按以下顺序构建最终生成指令，使用确定句，避免互相冲突的备选项：

1. 任务目标与生成方式：新生成、编辑、融合或扩展。
2. 参考图职责与必须保持的内容。
3. 相机：角度、高度、方向、焦段、景别、画幅、景深。
4. 主体：身份、外形、材质、服装、姿态、视线和空间锚点。
5. 场景：前景、中景、远景、背景及稳定拓扑。
6. 光影：真实光源、方向、软硬、反射光、互动光。
7. 色彩：主色数量、冷暖、饱和分配、高光和阴影色相。
8. 质感：真实摄影/动画/电商目标、细节预算、低噪点与自然边缘。
9. 负面约束：只写与当前高风险错误直接相关的禁止项。

把算力集中在人物脸、手、核心商品、关键道具和叙事动作。背景保持低频、干净，不要求所有区域同等精细。

## 直接生成或编辑图片

- 使用可用的图片生成技能和工具。首次看到本地参考图时先检查图片；不要凭文件名猜内容。
- 编辑任务明确写出“保持什么、只改什么”。新生成任务若用户说“不要在原图上优化/重新生成”，则把原图仅作为特征参考，生成全新画面，不做局部修补式编辑。
- 人物一致性任务把脸型、五官比例、发型、年龄感、肤色和服装分别锁定；不要只写“同一个人”。
- 空间一致性任务写明摄像机、主体和固定家具/建筑的相对位置；不要只写“保持构图”。
- 光影修正使用可追踪的相对变化，例如“当前暖黄强度降低约一半”，同时写明仍需保留的室内感，防止矫枉过正。
- 不为了显得高级而堆叠 `8K`、`ultra detailed`、强 HDR、极锐、过度磨皮等词。高清低噪点依靠可信曝光、干净暗部和受控细节实现。
- 生成后简短交付结果。只有用户要求时才附完整提示词或分析过程。

## 只输出提示词

默认中文。用户明确要求英文或目标模型更适合英文时输出英文。

默认采用 `prompt-framework.md` 的 24 字段顺序，确保信息完整。用户要求“简短、压缩、一段话、直接复制”或指定平台模板时，保持同一信息逻辑并压缩成单段，不强行展示字段。

多主体分别描述身份、外形、材质和动作；场景、相机、光线和色彩只写一次。不要把解释、备选方案和最终提示词混在同一个可复制文本中。

## 修正失败画面

优先判断错误属于哪一层：

- **理解错误**：参考图职责或“画面方向/现实空间方向”混淆。重写空间关系。
- **结构错误**：人数、肢体、座位、机位、构图、产品结构错误。减少无关描述并加强锚点。
- **动作错误**：同时出现多个动作或物理关系不清。拆成一个清晰状态或降低动作复杂度。
- **光影错误**：光源无来源、暗部死黑、过白、偏黄或主体与背景粘连。修改光源和反射关系。
- **质感错误**：过锐、塑料皮肤、噪点、脏灰、伪 HDR。降低数码处理感并重新分配细节。
- **风格错误**：真人、动漫或电商目标混杂。只保留一个主风格。

只修改导致错误的字段；结构性错误或用户明确要求“重新生成”时才整体重写。

## 成长机制

用户指出需要调整时，先完成当前修正，再按 `learning-system.md` 做学习价值判断；不要为了记录延迟当前任务，也不要把用户的幕后解释原样写进最终提示词。

单次经验先进入 `learning-inbox.md` 的观察或候选状态，不直接修改执行规则。只有通过独立验证，或用户明确批准为全局规则且因果、范围、例外清楚，才晋级到正确层：稳定视觉逻辑进入 `visual-decision-principles.md`，模型/平台经验进入 `active-learnings.md`，专项标准进入对应 reference，主流程变化才最小化修改 `SKILL.md`。

需要正式更新时调用 `skill-creator`，保留 `growth-ledger.md` 的判断历史；被新证据推翻的规则写入 `retired-learnings.md`。涉及商业路由、商品一致性或数字人互动时，使用 `evaluation-cases.md` 的对应任务验证。单次项目坐标、私人信息、未证实猜测和纯审美偶然值不得进入长期规则。

## 交付前检查

- 用户要“出图”还是“提示词”，是否执行了正确动作？
- 用户未要求灵感检索时，是否避免额外搜索与选择；要求时是否只给最多三个带示例图且差异明确的候选？
- 不可变内容是否逐项锁定？参考图职责是否清楚？
- 相机方向与现实空间方向是否分开表达？
- 主体、场景、动作和光源是否物理一致？
- 视觉中心是否唯一？细节是否集中在重点？
- 若为后续视频使用的场景母图，是否展示了足够多且可复用的场景特征，而非只得到一个局部近景？
- 暗部是否干净有层次，黑色材质是否仍有受光面？
- 是否避免过白、过黄、过锐、脏灰、噪点和无来源光效？
- 提示词是否只有一个确定方案，没有冲突词？
- 若有用户修正，是否完成成长价值判断，而不是机械记录？
